package com.trishit.optimite.ui.theme

import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.text.platform.Font

val GugiFont = FontFamily(
    Font(resource = "font/Gugi-Regular.ttf", weight = FontWeight.Normal)
)
val FunnelDisplay = FontFamily(
    Font(resource = "font/FunnelDisplay-Medium.ttf", weight = FontWeight.Normal)
)